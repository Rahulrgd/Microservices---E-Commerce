eureka:
    instance:
        hostname: localhost
    client:
        registery-with-eureka: false
        fetch-registry: false
        serviceUrl:
            defaultZone: http://{eureka.instance.hostname}/${server.port}/eureka/

server:
    port: 8761